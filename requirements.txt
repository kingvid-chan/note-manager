# Note Manager MVP — 依赖清单
# 实际运行复用 ~/外部需求/.conda/codingagent Conda 环境

fastapi>=0.100
uvicorn[standard]>=0.20
sqlalchemy>=2.0
bcrypt>=4.0
python-jose[cryptography]>=3.3
pydantic>=2.0
pydantic-settings>=2.0
pydantic[email]>=2.0
pytest>=7.0
httpx>=0.24
